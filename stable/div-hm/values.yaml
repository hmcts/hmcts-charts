nodejs:
    applicationPort: 3000
    ingressHost: "div-hm-{{ .Values.global.environment }}.service.core-compute-{{ .Values.global.environment }}.internal"
    environment:
        MONITOR_ENVIRONMENT: "aat"
