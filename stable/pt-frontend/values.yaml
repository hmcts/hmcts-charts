nodejs:
  applicationPort: 4000
  aadIdentityName: pt
  ingressHost: pt.{{ .Values.global.environment }}.platform.hmcts.net
  image: 'hmctsprod.azurecr.io/pt/frontend:latest'
  keyVaults:
    pt-kv1:
      secrets:
        - app-insights-connection-string
        - redis-connection-string
        - pt-session-secret
        - idam-system-user-name
        - idam-system-user-password
        - pt-frontend-idam-secret
        - pt-frontend-s2s-secret
  environment:
    PT_API_URL: 'http://pt-api-{{ .Values.global.environment }}.service.core-compute-{{ .Values.global.environment }}.internal'
    S2S_URL: 'http://rpe-service-auth-provider-{{ .Values.global.environment }}.service.core-compute-{{ .Values.global.environment }}.internal'

redis:
  enabled: false
  architecture: standalone
  master:
    persistence:
      enabled: false
  auth:
    enabled: false
