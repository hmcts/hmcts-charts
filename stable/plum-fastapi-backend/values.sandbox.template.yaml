# Don't modify this file, it is only needed for the pipeline to set the image and ingressHost
python:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    RECIPES_SERVICE_URL: http://plum-recipe-backend-sandbox.service.core-compute-aat.internal
